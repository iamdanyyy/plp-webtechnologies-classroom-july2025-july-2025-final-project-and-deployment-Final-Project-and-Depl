// JavaScript functionality specific to the services page
// This page only needs the common functionality, so this file is minimal

document.addEventListener('DOMContentLoaded', () => {
  // Services page specific functionality can be added here if needed
  // Currently, all functionality is handled by common.js
});
